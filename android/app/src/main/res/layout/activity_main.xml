<?xml version="1.0" encoding="utf-8"?>
<!--
  语音助手的样子：上面一个会"呼吸"的状态球（听/想/说三种颜色），中间是对话气泡，
  下面一个大按钮。服务器地址、权限这些不常用的收进「设置」里，默认不展开。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg"
    android:fitsSystemWindows="true">

    <!-- ── 顶栏 ── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="12dp"
        android:paddingTop="12dp"
        android:paddingBottom="4dp">

        <TextView
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/text"
            android:textSize="20sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/settings_btn"
            style="?attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:text="@string/settings"
            android:textColor="@color/primary"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- ── 状态球（听/想/说 一眼可辨）── -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="180dp">

        <View
            android:id="@+id/orb_glow"
            android:layout_width="170dp"
            android:layout_height="170dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_orb_glow"
            android:alpha="0.35" />

        <View
            android:id="@+id/orb"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_orb" />

        <TextView
            android:id="@+id/orb_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/orb_off"
            android:textSize="42sp" />
    </FrameLayout>

    <TextView
        android:id="@+id/status_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:text="@string/status_idle"
        android:textColor="@color/text"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/conn_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="6dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:text="@string/conn_idle"
        android:textColor="@color/text_dim"
        android:textSize="12sp" />

    <!-- ── 对话气泡 ── -->
    <TextView
        android:id="@+id/chat_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="24dp"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:text="@string/chat_empty"
        android:textColor="@color/text_dim"
        android:textSize="13sp" />

    <ScrollView
        android:id="@+id/chat_scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="10dp"
        android:clipToPadding="false">

        <LinearLayout
            android:id="@+id/chat_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="10dp" />
    </ScrollView>

    <!-- ── 设置面板（默认收起，展开时从按钮上方长出来）── -->
    <LinearLayout
        android:id="@+id/settings_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/bg_panel"
        android:padding="16dp">

        <EditText
            android:id="@+id/host_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/host_hint"
            android:inputType="textUri"
            android:importantForAutofill="no"
            android:textSize="14sp" />

        <Button
            android:id="@+id/discover_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/discover"
            android:textSize="13sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/connect_btn"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/connect"
                android:textSize="13sp" />

            <Button
                android:id="@+id/stop_btn"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/disconnect"
                android:textSize="13sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/permission_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:lineSpacingExtra="4dp"
            android:textSize="13sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/overlay_btn"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/grant_overlay"
                android:textSize="12sp" />

            <Button
                android:id="@+id/notify_access_btn"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/grant_notify_access"
                android:textSize="12sp" />
        </LinearLayout>

        <Button
            android:id="@+id/chat_clear_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/chat_clear"
            android:textSize="13sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/usage"
            android:textSize="12sp"
            android:textColor="@color/text_dim" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/hint_rom"
            android:textSize="12sp"
            android:textColor="@color/text_dim" />
    </LinearLayout>

    <!-- ── 底部操作 ── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="16dp">

        <Button
            android:id="@+id/voice_btn"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="56dp"
            android:backgroundTint="@color/primary"
            android:text="@string/voice_start"
            android:textColor="#FFFFFF"
            android:textSize="16sp" />

        <Button
            android:id="@+id/continuous_btn"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:layout_marginStart="10dp"
            android:text="@string/continuous_off"
            android:textSize="13sp" />
    </LinearLayout>
</LinearLayout>
